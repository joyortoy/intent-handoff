# No secrets are required for the public demo.
# The ranking engine is deterministic and uses a labeled demo catalog.
#
# Optional local-only override (never required in production):
# VITE_FORCE_DEMO_AGENT=true
